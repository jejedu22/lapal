{% extends 'base.html.twig' %}

{% block title %}Edit Boulanger{% endblock %}

{% block body %}
   <div class="col-md-12">
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">Modifier le boulanger : {{ boulanger.prenom }} {{ boulanger.nom }}</h3>
                <div class="card-tools">
                        {{ include('boulanger/_delete_form.html.twig') }}    
                </div>            
            </div>
            <!-- /.card-header -->
            <div class="card-body">

                {{ include('boulanger/_form.html.twig', {'button_label': 'Valider'}) }}
                <a href="{{ path('boulanger_index') }}"><i class="fas fa-angle-double-left"></i></a>
            </div>
        </div>
    </div>
{% endblock %}
