{% extends 'base.html.twig' %}

{% block title %}Boulanger{% endblock %}

{% block body %}
    <h1>Boulanger</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ boulanger.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ boulanger.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ boulanger.prenom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('boulanger_index') }}">back to list</a>

    <a href="{{ path('boulanger_edit', {'id': boulanger.id}) }}">edit</a>

    {{ include('boulanger/_delete_form.html.twig') }}
{% endblock %}
