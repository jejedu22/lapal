{% extends 'base.html.twig' %}

{% block title %}Paramètres{% endblock %}

{% block body %}
    <h1>Paramètres</h1>

    <table class="table">
        <tbody>
        {% for setting in settings %}
            <tr>
                <td>{{ setting.name |trans}}</td>
                    {% if setting.name == 'color'%}
                    <td><div class="option-index-color color-{{setting.value}}"></div></td>
                    {% elseif setting.name == 'logo'%}
                    <td><img src='{{ asset('uploads/logo/' ~ options_settings.get('logo')) }}' alt="logo" class="brand-image img-circle elevation-3 navbar-{{ options_settings.get('color') }}" style="opacity: .8; max-height: 50px;"></td>

                    {% else %}
                    <td>{{ setting.value }}</td>
                    {% endif %}
                <td>
                    <button type="button" class="btn btn-primary" onclick="location.href='{{ path('settings_edit', {'id': setting.id}) }}'">Modifier</button>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
