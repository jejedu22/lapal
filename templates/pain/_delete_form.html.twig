<form method="post" action="{{ path('pain_delete', {'id': pain.id}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pain.id) }}">
    <button class="btn" onclick="return confirm('Voulez-vous vraiment supprimer cette commande ?')"><i class="fas fa-trash-alt"></i></button>
</form>
