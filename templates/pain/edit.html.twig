{% extends 'base.html.twig' %}

{# {% form_theme form 'bootstrap_base_layout.html.twig' %} #}

{% block title %}Modifier le Pain{% endblock %}

{% block body %}
    <div class="col-md-6">
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">Modifier le Pain {{ pain.nom }} {{ pain.poid }} kg</h3>
                <div class="card-tools">
                        {{ include('pain/_delete_form.html.twig') }}    
                </div>            
            </div>
            <!-- /.card-header -->
            <div class="card-body">

                {{ include('pain/_form.html.twig', {'button_label': 'Valider'}) }}
                <a href="{{ path('pain_index') }}"><i class="fas fa-angle-double-left"></i></a>
            </div>
        </div>
    </div>


{% endblock %}
