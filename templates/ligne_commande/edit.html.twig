{% extends 'base.html.twig' %}

{% block title %}Modifier une ligne{% endblock %}

{% block body %}
    <div class="col-md-12">
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">Modifier une ligne</h3>
                <div class="card-tools">
                    {{ include('ligne_commande/_delete_form.html.twig') }}
                </div>            
            </div>
            <!-- /.card-header -->
            <div class="card-body">

               {{ include('ligne_commande/_form.html.twig', {'button_label': 'Valider'}) }}

                <a href="{{ path('ligne_commande_index') }}"><i class="fas fa-angle-double-left"></i></a>
            </div>
        </div>
    </div>
{% endblock %}
