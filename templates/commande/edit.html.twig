{% extends 'base.html.twig' %}

{% block title %}Modifier une commande{% endblock %}

{% block body %}
    <div class="col-md-12">
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">Modifier uns commande</h3>
                    {{ include('commande/_delete_form.html.twig') }}
                <div class="card-tools">
                </div>            
            </div>
            <!-- /.card-header -->
            <div class="card-body">
                {{ include('commande/_form.html.twig', {'button_label': 'Update'}) }}
                <a href="{{ path('commande_index') }}"><i class="fas fa-angle-double-left"></i></a>
            </div>
        </div>
    </div>
{% endblock %}
